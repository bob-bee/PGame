Role: Frontend UI Architect.
Mandate: Create a brutalist Quasar page.
Requirements:

Use <q-slider> for budget categories (e.g., Infrastructure, Education, Health). Ensure the total sum of all sliders equals 100%.

Implement a responsive grid for "Stance Voting" using <q-list> and <q-item> with a high-contrast black/white aesthetic.

Use Pinia state management for the budget calculations.

DEPLOYMENT NOTE: The generated DeliberationPage.vue must be included in the Quasar routing table. Ensure the package.json in the frontend root includes the necessary dependencies for <q-slider>