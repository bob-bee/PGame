System Global Directive:

Convention: All Python scripts must follow camelCase for functions and variables.

Design: High-contrast Brutalist UI (only black, white, and greyscale).

Architecture: Decentralized-first. Every component must be modular and designed for eventual federation via ActivityPub.

Safety: Always check if a target file exists before writing. If empty, initialize standard structure.

DATA ACCESS DIRECTIVE: All agents are granted read-only access to the Analytics and Treasury tables. When calculating simulations, agents must reference the config/sim_parameters.json for weightings.

What is your priority for this level?

The "What-If" Engine: Focus on the SimulationEngine so the community can see the consequences of their choices before they vote.

The "Automated Execution": Focus on the ActionExecutor so that once a vote is finished, the platform automatically initiates the budget transfer.